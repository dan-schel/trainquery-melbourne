export const CITY_LOOP = 1;
export const CITY_LOOP_UNDERGROUND = 2;
export const METRO_TUNNEL = 3;
export const SPECIAL_EVENTS_ONLY = 4;

// Stops tags are used to configure custom stop departure feeds, e.g. the
// underground city loop stations group departures by platform. (Other uses
// might come later too, hence the SPECIAL_EVENTS_ONLY tag.)
